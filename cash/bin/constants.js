const API = 'https://api.exchangeratesapi.io/latest';
const DEFAULT_TO_CURRENCIES = ['USD', 'EUR', 'GBP', 'JPY'];
const DEFAULT_TO_CURRENCIES = ['USD', 'EUR', 'DH', 'CNY'];

module.exports = {
  API,
  DEFAULT_TO_CURRENCIES
}
